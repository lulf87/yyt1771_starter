Runtime outputs for development profiles live here so the project root keeps the
top-level structure frozen to `configs/`, `docs/`, `examples/`, `src/`, and
`tests/`.

Ignored generated contents:

- `*.sqlite3`
- `artifacts/`
