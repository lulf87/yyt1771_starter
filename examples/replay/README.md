# Replay Dataset

This dataset is a minimal offline replay sample for the YYT1771 web demo.

- `temp_series.csv` contains timestamped temperature values.
- `frames/*.json` contains timestamped grayscale image arrays.

The three samples are designed so the current minimal vision extractor produces
a valid monotonic displacement curve that can reach Af95.
